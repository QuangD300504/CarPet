package com.example.vetbook.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──────────────────────────────────────────────
val Brand         = Color(0xFF14B8A6) // Primary Teal accent
val BrandDark     = Color(0xFF0D9488) // Pressed / darker Teal
val BrandSurface  = Color(0xFFF0FDFA) // Tinted bg for chips, pills

// ── Healthcare / Pro Max ───────────────────────────────
val HealthPrimary = Color(0xFF164E63) // Deep Calm Cyan for trust
val HealthSurface = Color(0xFFECFDF5) // Very light teal/cyan for bgs
val HealthMuted   = Color(0xFF64748B) // Slate for muted text

// ── Neutral surface ────────────────────────────────────
val SurfaceColor  = Color(0xFFFFFFFF) // Card / screen surface
val Background    = Color(0xFFF7F8FA) // Light grey page background

// ── Text ───────────────────────────────────────────────
val TextPrimary   = Color(0xFF1A1A2E) // Deep navy-black headlines
val TextSecondary = Color(0xFF6B7280) // Subtext / captions

// ── Semantic ───────────────────────────────────────────
val Divider       = Color(0xFFEEEFF2) // Subtle borders
val Success       = Color(0xFF22C55E) // Payment success
val Error         = Color(0xFFEF4444) // Errors, logout
val Link          = Color(0xFF3B82F6) // Links, profile badge values
